import styled from 'styled-components';
import { shadow } from '../variables';

export const Card = styled.div`
    background: white;
    margin: 8px 0;
    box-shadow: ${shadow};
    padding: 24px 32px;
`